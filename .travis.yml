language: node_js
node_js:
- lts/*
script:
- npm run build
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: public # 项目打包后生成的文件的目录
  github-token: $GITHUB_TOKEN
  target-branch: gh-pages-source
  keep-history: true
  on:
    branch: master
branches:
  only:
  - master